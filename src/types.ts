
/* MAIN */

type Options = {
  exclude: string[],
  ignore: string[],
  include: string[],
  showUntitledFiles: boolean,
  showOpenFiles: boolean,
  showFoundFiles: boolean,
  showFoundRelativeFiles: boolean
};

/* EXPORT */

export type {Options};
